<h1><%= @movie.title %></h1>
<p><%= @movie.description %></p>

<p><%= link_to 'Edit', edit_movie_path(@movie) %></p>
<p><%= link_to 'Delete Movie', @movie, method: :delete, data: { confirm: 'Are you sure?' } %></p>